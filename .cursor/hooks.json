{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "node \"$(git rev-parse --show-toplevel)/hooks/protect-important-paths.js\"",
        "matcher": ".*"
      }
    ]
  }
}
